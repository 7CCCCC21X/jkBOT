services:
  bot:
    build: .
    image: jkbot:latest
    container_name: jkbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      # 命名卷, 重新部署 / 重建容器不会丢失 tokens.json 与 history.json
      - jkbot_data:/data

volumes:
  jkbot_data:
